apiVersion: v1
kind: ConfigMap
metadata:
  name: bsf-config
  namespace: open5gs

data:
  bsf.yaml: |
    logger:
      level: info

    bsf:
      sbi:
        server:
          - address: POD_IP
            port: 7777

        client:
          scp:
            - uri: http://open5gs-scp:7777
