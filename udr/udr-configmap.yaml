apiVersion: v1
kind: ConfigMap
metadata:
  name: udr-config
  namespace: open5gs

data:
  udr.yaml: |
    logger:
      level: info

    udr:
      sbi:
        server:
          - address: POD_IP
            port: 7777

        client:
          scp:
            - uri: http://open5gs-scp:7777
