apiVersion: v1
kind: ConfigMap
metadata:
  name: udm-config
  namespace: open5gs

data:
  udm.yaml: |
    logger:
      level: info

    udm:
      sbi:
        server:
          - address: POD_IP
            port: 7777

        client:
          scp:
            - uri: http://open5gs-scp:7777
